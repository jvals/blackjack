package app;

public class Main {

    public static void main(String[] args) {
        // Read from commandline args
        // If file is specified, read file and convert the contents to an array of cards
        // Else, generate a new shuffled deck of cards
        // Create a new game, with the array of cards as input
        // Run the game
    }
}
